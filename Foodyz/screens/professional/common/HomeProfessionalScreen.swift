import SwiftUI

struct HomeProfessionalView: View {
    var body: some View {
        VStack {
            Text("Hello, Professional!")
                .font(.largeTitle)
                .fontWeight(.bold)
                .padding()
            
            Spacer()
        }
    }
}

#Preview {
    HomeProfessionalView()
}
